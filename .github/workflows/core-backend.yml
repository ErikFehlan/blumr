name: Validate and deploy dependable core
on:
  workflow_call:
  workflow_dispatch:
permissions:
  contents: read
jobs:
  validate:
    runs-on: ubuntu-latest
    services:
      postgres:
        image: postgres:16
        env:
          POSTGRES_PASSWORD: postgres
        ports: ['5432:5432']
        options: --health-cmd pg_isready --health-interval 5s --health-timeout 5s --health-retries 10
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
      - uses: denoland/setup-deno@v2
        with:
          deno-version: v2.x
      - name: Check core code and authenticated handlers
        run: |
          node --test tests/*.test.js
          deno check supabase/functions/account-controls/index.ts supabase/functions/reassess-job/index.ts supabase/functions/analyze-patterns-beta/index.ts supabase/functions/analyze-patterns-v2/index.ts supabase/functions/sol-model-check/index.ts
          deno test --allow-env tests/sol-routing.test.ts tests/assessment-memory-routing.test.ts tests/security-backend.test.ts tests/learning-routing.test.ts tests/account-controls.test.ts tests/analysis-latency.test.ts tests/resume-intake-backend.test.ts tests/job-reassessment-worker.test.ts tests/hiring-priorities-worker.test.ts
      - name: Check database transactions and permissions
        env:
          PGPASSWORD: postgres
        run: |
          psql -h localhost -U postgres -d postgres -f tests/criteria-queue.sql
          createdb -h localhost -U postgres core_intake
          psql -h localhost -U postgres -d core_intake -f tests/core-intake.sql
          createdb -h localhost -U postgres assessment_memory
          psql -h localhost -U postgres -d assessment_memory -f tests/assessment-memory.sql
          createdb -h localhost -U postgres hiring_priorities
          psql -h localhost -U postgres -d hiring_priorities -f tests/hiring-priorities.sql
          createdb -h localhost -U postgres usage_analytics
          psql -h localhost -U postgres -d usage_analytics -f tests/usage-analytics.sql
          node tests/usage-migration-contention.mjs
          createdb -h localhost -U postgres feedback_learning
          psql -h localhost -U postgres -d feedback_learning -f tests/learning.sql
          createdb -h localhost -U postgres beta_security
          psql -h localhost -U postgres -d beta_security -f tests/beta-security.sql
          node tests/security-concurrency.mjs
  deploy:
    needs: validate
    if: github.ref == 'refs/heads/main'
    runs-on: ubuntu-latest
    environment: criteria-backend
    concurrency:
      group: criteria-backend-deploy
      cancel-in-progress: false
    timeout-minutes: 25
    env:
      SUPABASE_ACCESS_TOKEN: ${{ secrets.SUPABASE_ACCESS_TOKEN }}
      SUPABASE_PROJECT_REF: ${{ vars.SUPABASE_PROJECT_REF }}
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
      - uses: supabase/setup-cli@v1
        with:
          version: latest
      - name: Deploy temporary private model validation
        id: sol_probe
        run: supabase functions deploy sol-model-check --project-ref "$SUPABASE_PROJECT_REF"
      - name: Verify Sol access and compare synthetic cases before production rollout
        run: node scripts/sol-preflight.mjs
      - name: Remove temporary model validation
        if: always() && steps.sol_probe.outcome == 'success'
        run: supabase functions delete sol-model-check --project-ref "$SUPABASE_PROJECT_REF" --yes
      - name: Prepare durable processing
        run: |
          node scripts/job-reassessment-backend.mjs prepare
          node scripts/core-backend.mjs
          node scripts/account-controls-backend.mjs
          node scripts/usage-analytics-backend.mjs
          node scripts/learning-backend.mjs
          node scripts/security-backend.mjs prepare
      - name: Deploy authenticated analysis and durable worker
        run: |
          supabase functions deploy analyze-patterns-v2 --project-ref "$SUPABASE_PROJECT_REF"
          supabase functions deploy analyze-patterns-beta --project-ref "$SUPABASE_PROJECT_REF"
          supabase functions deploy reassess-job --project-ref "$SUPABASE_PROJECT_REF"
          supabase functions deploy refine-job-criteria --project-ref "$SUPABASE_PROJECT_REF"
          supabase functions deploy account-controls --project-ref "$SUPABASE_PROJECT_REF"
          supabase secrets set ASSESSMENT_MODEL=gpt-5.6-sol FEEDBACK_MODEL=gpt-5.6-sol REASSESSMENT_MODEL=gpt-5.6-sol --project-ref "$SUPABASE_PROJECT_REF"
          node scripts/usage-analytics-backend.mjs activate
      - name: Verify worker transport and activate scheduler
        run: node scripts/job-reassessment-backend.mjs activate
      - name: Verify live account and document isolation
        run: |
          node scripts/security-backend.mjs verify
          node scripts/core-live-smoke.mjs
          node scripts/security-live-smoke.mjs
          node scripts/usage-analytics-backend.mjs verify
      - name: Recover previously failed resume quotations
        run: node scripts/recover-resume-intake.mjs
